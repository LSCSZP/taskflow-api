# Runtime User Verification

Before applying any patches, verify the agent runtime user context by running:

    bash verify-runtime-user.sh

This confirms patch operations will run with the correct permissions.
